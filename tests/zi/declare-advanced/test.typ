#set page(width: auto, height: auto, margin: .5em)
#import "/src/zero.typ": set-unit, zi

#set-unit(fraction: "inline")

#zi.declare(($Pi$, 2), alt: "Pi")() \
#zi.declare($M_dot.o$, ("s", -2), ($β$, -2), alt: "Solar masses per seconds squared per beta squared")() \
#zi.declare(($M_dot.o$, -1), ("s", -0.5), ($β$, "-a"), alt: "")() \
#zi.declare((sym.prime.double, 2))() \
